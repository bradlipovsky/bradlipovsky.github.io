services:
  site:
    build: .
    command: bundle exec jekyll serve --host 0.0.0.0 --livereload --force_polling --config _config.yml,_config.dev.yml
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/site
